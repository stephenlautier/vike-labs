import type { Config } from "vike/types";

export default {
	title: "My Profile — Rift",
	description: "Your League of Legends player profile, owned champions, and match history",
	// `passToClient` is configured at the root +config.ts; user lives on
	// pageContext from the Auth.js handler.
} satisfies Config;
