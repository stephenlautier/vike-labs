// Public barrel for the @rift/mfe-player package. Stencil-generated bundles
// (dist, dist-custom-elements, hydrate, src/react) are produced from this
// folder; this barrel exists so consumers that import the package without a
// sub-path can still resolve to a valid module.
export {};
