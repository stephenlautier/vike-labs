// Component type declarations — always available (committed to source).
export type { Components } from "./components";
// React wrappers are exposed via the @rift/ui/react subpath (libs/ui/src/react/components.ts).
// Import from @rift/ui/react in apps rather than directly from @rift/ui.
